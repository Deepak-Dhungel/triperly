/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "maps.googleapis.com",
      "lh3.googleusercontent.com",
      "example.com",
    ],
  },
};

export default nextConfig;
