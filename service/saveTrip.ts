import { User } from "firebase/auth";

export const handleSaveTrip = (isLoggedIn: User|undefined) => {

   //check if user is logged in
   if(!isLoggedIn){

   }

   //if not logged in, take to login page

   //else add data to firestore db

}